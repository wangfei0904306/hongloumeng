<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.waskj.hongloumeng.main.BaseActivity">


    <com.waskj.hongloumeng.common.MyScrollView
        android:id="@+id/frag_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbarStyle="outsideOverlay"
        android:scrollbarThumbVertical="@color/green"
        android:scrollbarSize="1dip" >

        <TextView
            android:id="@+id/frag_content"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="50dip"
            android:paddingBottom="50dip"
            android:paddingLeft="10dip"
            android:paddingRight="4dip"
            android:lineSpacingMultiplier="1.6"

            android:textColor="@color/black"
            android:textSize="19sp"
            android:text="sanguo"/>

    </com.waskj.hongloumeng.common.MyScrollView>

    <TextView
        android:id="@+id/frag_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="10dip"
        android:paddingBottom="10dip"
        android:paddingLeft="6dip"
        android:paddingRight="6dip"
        android:background="@color/miyellow"
        android:maxLines="1"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:drawableRight="@mipmap/title_right_menu"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold"
        android:text="三国演义"/>

    <ImageView
        android:id="@+id/up_top_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="50dip"
        android:layout_marginRight="30dip"
        android:src="@mipmap/up_top"/>



</RelativeLayout>
